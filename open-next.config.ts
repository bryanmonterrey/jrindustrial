import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// OpenNext adapter config — the same Next-on-Cloudflare-Workers path the
// podbot site uses. No bindings yet.
export default defineCloudflareConfig({});
